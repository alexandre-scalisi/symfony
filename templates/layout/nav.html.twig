<div class="h-20 bg-green-500 flex items-center justify-between px-8">
    <div class="flex items-center gap-6">
        <h3 class="text-2xl font-bold text-blue-600">Blog</h3>
        <ul class="inline">
            <li><a href="{{ path('article_index') }}" class="text-xl font-bold">Articles</a></li>
        </ul>
    </div>
    <div>
        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
            <a href="{{ path('app_login') }}" class="mr-3">Se connecter</a>
            <a href="{{ path('app_register') }}">S'enregistrer</a>
        {% else %}
            <a href="{{ path('app_logout') }}">Se déconnecter</a>
        {% endif %}

    </div>
</div>

